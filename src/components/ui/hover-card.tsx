"use client";

import * as React from "react";

// Stub component - hover-card not used in this project
// To use this component, install: npm install @radix-ui/react-hover-card

export function HoverCard({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

export function HoverCardTrigger({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

export function HoverCardContent({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
